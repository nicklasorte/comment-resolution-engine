- rule_id: ISSUE_ALREADY_ADDRESSED
  priority: 90
  enabled: true
  match:
    text_contains: "already addressed in later revision"
  action:
    issue_type: "already_addressed_later_revision"
    disposition: "Reject"
    notes: "Handled in later revision."
