# Examples

This directory contains non-binary CSV examples to document expected input/output column structure.

- `sample_comment_matrix.csv`: sample input matrix shape.
- `sample_output.csv`: sample output resolution table shape.

Convert CSV to XLSX locally if you want to run the CLI with these examples.
