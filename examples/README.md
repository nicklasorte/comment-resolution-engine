# Examples

This directory contains non-binary CSV examples to document the expected NTIA matrix shape.

- `sample_comment_matrix.csv`: sample input matrix using the NTIA headers.
- `sample_resolved_output.csv`: sample output with NTIA Comments, Comment Disposition, and Resolution populated.

Convert CSV to XLSX locally if you want to run the CLI with these examples.
